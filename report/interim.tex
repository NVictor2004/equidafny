\documentclass[a4paper, 11pt]{article}
\usepackage{graphicx} % Required for inserting images

% Layout stuff, 1in is standard margin
\usepackage[margin=1in]{geometry}

% Bibliography settings
\usepackage[backend=biber,style=ext-authoryear,uniquename=false]{biblatex}
\let\cite\parencite
\DeclareOuterCiteDelims{parencite}{\bibopenbracket}{\bibclosebracket}
\DeclareInnerCiteDelims{textcite}{\bibopenbracket}{\bibclosebracket}
\addbibresource{references.bib}

% Code listing settings
\usepackage{listings}
\usepackage{xcolor}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\lstdefinestyle{codestyle}{
    backgroundcolor=\color{backcolour},   
    basicstyle=\ttfamily\footnotesize
}
\lstset{style=codestyle}

% Hyperlink settings
\usepackage{hyperref}
% Define colours
\definecolor[named]{ACMPurple}{cmyk}{0.55,1,0,0.15}
\definecolor[named]{ACMDarkBlue}{cmyk}{1,0.58,0,0.21}
\hypersetup{colorlinks,
    % This colours internal (document) links as purple
    % and external links as blue 
    linkcolor=ACMPurple,
    citecolor=ACMPurple,
    urlcolor=ACMDarkBlue,
    filecolor=ACMDarkBlue}

\title{Interim Report}
\author{Nathaniel Victor}
\date{\today}

\begin{document}

\maketitle

% Using induction on the recursive structure of the functions
% when functions are not recursive, we directly encode equivalence lemmas into SMT solver formulas. 

% Internally, Stainless operates on abstract syntax trees, that are transformed throughout several
% pipeline phases. This series of transformations results in verification conditions that are then sent
% to an SMT solver.
\section{Existing Program Verifiers}
\subsection{Dafny}

As described in \textcite{leinoDafnyAutomaticProgram2010}, Dafny is primarily a programming language, containing many of the classic language features and constructs we expect from a modern programming language. However, in addition to this, it includes specification constructs, such as preconditions, postconditions and loop invariants, to allow Dafny's program verifier to automatically prove that a given program matches its specification. Dafny can also be compiled into a variety of other languages, including C\#, Java and Python, allowing verified programs to be integrated easily into existing codebases.

Dafny's program verification heavily relies on Boogie \parencite{10.1007/11804192_17}, another state-of-the-art program verifier. The verification process involves translating a Dafny program into a Boogie program; this is then translated into a series of verification conditions, which are passed to the Z3 SMT solver \parencite{10.1007/978-3-540-78800-3_24}. 

\section{Dafny Demonstration}

In this section, I will provide examples of Dafny functions that attempt to check whether a list is sorted. I have converted these examples into Dafny from the original Scala examples provided by \textcite{milovancevicProvingDisprovingEquivalence2023}. There will be one reference solution and a series of student solutions where Dafny must prove or disprove their equivalence to the reference solution. The goal of this demonstration is to show Dafny's capabilities and limitations in this field.

\subsection{Necessary Datastructures}

I will require the following definition of a list data structure:
\begin{lstlisting}
datatype List<T> = Nil | Cons(head: T, tail: List<T>)
\end{lstlisting}

\hypertarget{egr}{
\subsection{Reference Solution}
}

The reference solution for this example is detailed below, involving a helper function \lstinline|loop| and the main function \lstinline|isSortedR|. 
\begin{lstlisting}
function loop(p: int, l: List<int>): bool
  decreases l
{
  match l
  case Nil => true
  case Cons(x, xs) => p <= x && loop(x, xs)
}

function isSortedR(l: List<int>): bool
{
  match l
  case Nil => true
  case Cons(x, xs) => loop(x, xs)
}
\end{lstlisting}

\hypertarget{ega}{
\subsection{Student A's Solution}
}

The first student solution is as follows:
\begin{lstlisting}
function leq(cur: int, next: int): bool
{
  cur < next
}

function iter(l: List<int>): bool
{
  match l
  case Nil => true
  case Cons(x, Nil) => true
  case Cons(x, Cons(y, ys)) => leq(x, y) && iter(Cons(y, ys))
}

function isSortedA(l: List<int>): bool
{
  match l
  case Nil => true
  case Cons(_, Nil) => true
  case Cons(x, Cons(y, ys)) => x <= y && iter(Cons(y, ys))
}
\end{lstlisting}
%
This solution is non-equivalent to the reference solution, as it uses a strictly less-than comparison in the \lstinline|leq| function, when it should use a less-than-or-equal-to comparison. Therefore, Dafny is unable to prove the following equivalence lemma:
\begin{lstlisting}
lemma isSortedEquivalence(l: List<int>)
  ensures isSortedA(l) == isSortedR(l)
{}
\end{lstlisting}
%
However, it incorrectly provides \lstinline|Cons(-38, Cons(7681, Nil))| as a counterexample. I extended the above proof to include and use a helper lemma, as shown below:
\begin{lstlisting}
lemma isSortedEquivalenceHelper(x: int, xs: List<int>)
  decreases xs
  ensures iter(Cons(x, xs)) == loop(x, xs)
{}

lemma isSortedEquivalence(l: List<int>)
  ensures isSortedA(l) == isSortedR(l)
{
  match l
  case Nil => {}
  case Cons(x, Nil) => {}
  case Cons(x, Cons(y, ys)) => isSortedEquivalenceHelper(y, ys);
}
\end{lstlisting}
%
Now, Dafny is able to provide a correct counterexample of \lstinline|x = 8855| and \lstinline|xs = Cons(8855, Nil)| for the helper lemma. However, this is not the simplest counterexample possible, as \lstinline|x = 0| and \lstinline|xs = Cons(0, Nil)| would be sufficient.

\hypertarget{egb}{
\subsection{Student B's Solution}
}

The second student solution is as follows:
\begin{lstlisting}
function isSortedB(l: List<int>): bool
{
  match l
  case Nil => true
  case Cons(x, Nil) => isSortedB(Nil)
  case Cons(x, Cons(y, ys)) => x <= y && isSortedB(Cons(y, ys))
}
\end{lstlisting}
%
This solution is equivalent to the reference solution. Dafny is able to prove this without any additional help, through the following equivalence lemma:
\begin{lstlisting}
lemma isSortedEquivalence(l: List<int>)
  ensures isSortedR(l) == isSortedB(l)
{}
\end{lstlisting}

\hypertarget{egc}{
\subsection{Student C's Solution}
}

The third student solution is as follows:
\begin{lstlisting}
function chk(l: List<int>, p: int, a: bool): bool
{
  match l
  case Nil => a
  case Cons(x, xs) => x >= p && chk(xs, x, a)
}

function isSortedC(l: List<int>): bool
{
  match l
  case Nil => true
  case Cons(x, xs) => chk(Cons(x, xs), x, true)
}
\end{lstlisting}
%
This example is equivalent to the reference solution, however, Dafny requires a helper lemma to prove this:
\begin{lstlisting}
lemma isSortedEquivalenceHelper(x: int, xs: List<int>)
  decreases xs
  ensures chk(xs, x, true) == loop(x, xs)
{}

lemma isSortedEquivalence(l: List<int>)
  ensures isSortedC(l) == isSortedR(l)
{
  match l
  case Nil => {}
  case Cons(x, xs) => isSortedEquivalenceHelper(x, xs);
}
\end{lstlisting}

\subsection{Summary of findings from Demonstration}
This example proves that Dafny is capable enough to prove program equivalence in some cases without any help, as with \hyperlink{egb}{Student B's solution}. However, as with \hyperlink{egc}{Student C's solution}, Dafny requires additional helper lemmas to be provided to prove the equivalence of helper functions. It also requires that these helper lemmas are explicitly called within the main equivalence lemma. Finally, as with \hyperlink{ega}{Student A's solution}, Dafny can sometimes provide incorrect counterexamples for non-equivalent functions. When provided with helper lemmas, the tool will eventually provide a correct counterexample, but not necessarily the simplest one.

\subsection{Possible solutions}
\subsubsection{Proving Equivalence of Auxiliary Lemmas}

\textcite{milovancevicProvingDisprovingEquivalence2023} solved this problem by preprocessing the student and reference solutions to find functions with compatible signatures: where the arguments of one function can be permuted so that their types match the types of the arguments of another function. Once candidate pairs of functions are found, equivalence lemmas can be automatically generated and proven. If multiple functions share the same signature, test cases are used to narrow down the search space. These can be defined by the user or generated from counterexamples found when disproving equivalence of other incorrect candidate submissions.

\subsubsection{Generating Simpler Counterexamples}

As asserted by \textcite{putrueleMaskDToolMeasuring2022}, since there are multiple translations steps between the Dafny source code and the verification conditions sent to the Z3 SMT solver, the counterexamples produced by it can be difficult to read and understand. They also note these counterexamples may sometimes be incorrect, as seen in \hyperlink{ega}{Student A's solution}, due to the Z3 solver being incomplete.

TODO

\printbibliography
\end{document}



